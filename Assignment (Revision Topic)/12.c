#include<stdio.h>
int main()
{
   int side, area;
   printf("Enter the Length of side   : ");
   scanf("%d", &side);
   area = (side * side);
   printf("Area of the Square is : %d", area);

   return 0;
}
